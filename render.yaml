services:
  - type: web
    name: esg-analyzer
    runtime: python
    buildCommand: |
      pip install flask pdfplumber pandas spacy
      python -m spacy download en_core_web_sm
    startCommand: python app.py
    envVars:
      - key: FLASK_ENV
        value: production
